import { DomeLayerConfig } from "../types";

// Define your "layers" (example config)
export const domeLayers: DomeLayerConfig[] = [
  { angleDeg: 30, radius: 10, screenCount: 8 },
  { angleDeg: 60, radius: 12, screenCount: 12 },
  { angleDeg: 80, radius: 14, screenCount: 8 },
];
